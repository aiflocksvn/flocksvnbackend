# phone_number_regex =
# phoneNumberRegex = RegexValidator(regex=phone_number_regex)
#
#
# def valida_phone_num(value):
#     pat = re.compile(phone_number_regex)
#     if re.fullmatch(pat, value):
#
#     else:
#         print(f"'{value}' is NOT a alphanumeric string!")
#         raise serializers.ValidationError('incorrect phone number format')
